[project]
name = "simforge-ai-copilot"
version = "0.1.0"
description = "AI assistant service for code generation, diagnostics, and design suggestions."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
  "pydantic>=2.8",
]

[tool.uvicorn]
factory = false
