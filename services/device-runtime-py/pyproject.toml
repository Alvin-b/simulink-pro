[project]
name = "simforge-device-runtime"
version = "0.1.0"
description = "Device runtime bridge for firmware artifacts and simulated hardware bindings."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
  "pydantic>=2.8",
]
